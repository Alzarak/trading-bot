{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/install-deps.sh\"",
            "timeout": 120
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/validate-order.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if a trading session was active in this conversation. If trading occurred, verify all positions have stop-losses and note any pending signals, then return {\"decision\": \"block\", \"reason\": \"reason\"}. If safe or no trading occurred, return {\"decision\": \"allow\", \"reason\": \"Safe to stop\"}.",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
