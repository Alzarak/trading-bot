---
phase: quick
plan: 260322-14r
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/hooks.json
  - scripts/install-deps.sh
autonomous: true
must_haves:
  truths:
    - "SessionStart hook does not error when CLAUDE_PLUGIN_ROOT is unset (dev mode)"
    - "SessionStart hook still installs deps correctly when CLAUDE_PLUGIN_ROOT is set (plugin mode)"
  artifacts:
    - path: "scripts/install-deps.sh"
      provides: "Guarded dependency installer that handles dev vs plugin context"
    - path: "hooks/hooks.json"
      provides: "Hook configuration (verify structure is correct)"
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/install-deps.sh"
      via: "SessionStart command"
      pattern: "install-deps\\.sh"
---

<objective>
Fix the SessionStart hook error that occurs when opening the trading-bot project in Claude Code.

Purpose: The install-deps.sh script called by the SessionStart hook crashes because `CLAUDE_PLUGIN_ROOT` is unset in development context (the project is open as source code, not installed as a plugin). The script then resolves `requirements.txt` to `/requirements.txt`, which does not exist, and `set -e` causes it to exit with error code 1.

Output: A resilient install-deps.sh that gracefully handles both development mode (CLAUDE_PLUGIN_ROOT unset) and installed-plugin mode (CLAUDE_PLUGIN_ROOT set).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/hooks.json
@scripts/install-deps.sh
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-mode guard to install-deps.sh</name>
  <files>scripts/install-deps.sh</files>
  <action>
Add a guard at the top of install-deps.sh (after the PATH export, before any PLUGIN_ROOT usage) that detects when CLAUDE_PLUGIN_ROOT is unset or empty. When unset:

1. Fall back to the script's own directory to find the project root: `PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"` — this resolves to the repo root since the script lives in `scripts/`.
2. Similarly, if CLAUDE_PLUGIN_DATA is unset, fall back to a sensible default: `DATA_DIR="${CLAUDE_PLUGIN_DATA:-${PLUGIN_ROOT}/.plugin-data}"` — keeps venv and hash file local to the project during development.
3. Add `mkdir -p "${DATA_DIR}"` (already exists but ensure it comes after the fallback assignment).
4. After the fallback, verify requirements.txt actually exists before trying to hash it: `if [ ! -f "${REQ_FILE}" ]; then echo "[trading-bot] requirements.txt not found at ${REQ_FILE}, skipping dependency install."; exit 0; fi`

Do NOT change the plugin-mode behavior — when CLAUDE_PLUGIN_ROOT is set, the script must behave exactly as before. The fallback only activates when the env var is empty/unset.

Also ensure validate-order.sh (hooks/validate-order.sh) has a similar resilience pattern for CLAUDE_PLUGIN_DATA — it already uses `${CLAUDE_PLUGIN_DATA:-/tmp}` which is fine, no changes needed there.
  </action>
  <verify>
    <automated>bash -c 'unset CLAUDE_PLUGIN_ROOT; unset CLAUDE_PLUGIN_DATA; bash /home/parz/projects/trading-bot/scripts/install-deps.sh' 2>&1 | grep -E "(up to date|installed|skipping)" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>install-deps.sh exits 0 (no error) both when CLAUDE_PLUGIN_ROOT is set and when it is unset. When unset, it falls back to the script's parent directory as project root.</done>
</task>

<task type="auto">
  <name>Task 2: Validate hooks.json structure and test end-to-end</name>
  <files>hooks/hooks.json</files>
  <action>
Verify the hooks.json structure is valid per Claude Code hooks spec. The current structure uses the correct format (matches the global settings.json pattern). Confirm:

1. The SessionStart hook command string correctly references `${CLAUDE_PLUGIN_ROOT}/scripts/install-deps.sh` — this is fine for plugin mode, and the script's own fallback now handles dev mode.
2. Run `jq . hooks/hooks.json` to validate JSON syntax.
3. If any structural issues are found, fix them. If the structure is correct (which it appears to be), leave hooks.json unchanged.

No changes expected to hooks.json itself — the fix is entirely in install-deps.sh. This task is verification-only.
  </action>
  <verify>
    <automated>jq . /home/parz/projects/trading-bot/hooks/hooks.json > /dev/null 2>&1 && echo "JSON valid" || echo "JSON invalid"</automated>
  </verify>
  <done>hooks.json is valid JSON with correct Claude Code hooks structure. The SessionStart hook combined with the updated install-deps.sh no longer produces errors in dev mode.</done>
</task>

</tasks>

<verification>
1. `unset CLAUDE_PLUGIN_ROOT && bash scripts/install-deps.sh` exits 0
2. `CLAUDE_PLUGIN_ROOT=/home/parz/projects/trading-bot CLAUDE_PLUGIN_DATA=/tmp/tb-test bash scripts/install-deps.sh` exits 0
3. `jq . hooks/hooks.json` succeeds
</verification>

<success_criteria>
- Opening the trading-bot project in Claude Code no longer triggers a SessionStart hook error
- The install-deps.sh script gracefully handles both development (no CLAUDE_PLUGIN_ROOT) and plugin (CLAUDE_PLUGIN_ROOT set) contexts
- No changes to the plugin-mode behavior — installed users are unaffected
</success_criteria>

<output>
After completion, create `.planning/quick/260322-14r-fix-the-session-startup-hook-error/260322-14r-SUMMARY.md`
</output>
