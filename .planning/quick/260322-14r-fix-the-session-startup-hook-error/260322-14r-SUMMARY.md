---
phase: quick
plan: 260322-14r
subsystem: infra
tags: [bash, hooks, plugin, dev-mode, install-deps]

requires: []
provides:
  - "install-deps.sh that handles both dev mode (CLAUDE_PLUGIN_ROOT unset) and plugin mode (CLAUDE_PLUGIN_ROOT set)"
  - "SessionStart hook no longer errors when opening trading-bot as source code in Claude Code"
affects: [hooks, plugin-distribution]

tech-stack:
  added: []
  patterns:
    - "Shell env-var fallback pattern: PLUGIN_ROOT=${VAR:-$(cd dirname/.. && pwd)} for dev/plugin dual-context scripts"
    - "Guard requirements.txt existence before sha256sum to prevent crash on missing file"

key-files:
  created: []
  modified:
    - scripts/install-deps.sh

key-decisions:
  - "Fall back to script's parent directory as PLUGIN_ROOT when CLAUDE_PLUGIN_ROOT is unset — script lives in scripts/, so dirname/.. resolves to repo root"
  - "Fall back to .plugin-data/ within project root when CLAUDE_PLUGIN_DATA is unset — keeps dev venv local and separate from installed-plugin state"
  - "Early exit (exit 0) when requirements.txt is missing — informative message, no error — handles edge cases in CI or stripped checkouts"

requirements-completed: []

duration: 5min
completed: 2026-03-22
---

# Quick Task 260322-14r: Fix Session Startup Hook Error Summary

**install-deps.sh now handles dev mode via env-var fallbacks so the SessionStart hook no longer crashes when CLAUDE_PLUGIN_ROOT is unset**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-22T14:00:00Z
- **Completed:** 2026-03-22T14:05:00Z
- **Tasks:** 2 (1 fix, 1 verification-only)
- **Files modified:** 1

## Accomplishments

- Added fallback resolution of PLUGIN_ROOT to the script's parent directory when CLAUDE_PLUGIN_ROOT is not set
- Added fallback resolution of DATA_DIR to .plugin-data/ inside the project root when CLAUDE_PLUGIN_DATA is not set
- Added early-exit guard when requirements.txt does not exist at the resolved path
- Verified hooks.json is valid JSON and requires no structural changes
- All three plan verification tests pass: dev mode exits 0, plugin mode exits 0, jq validates hooks.json

## Task Commits

1. **Task 1: Add dev-mode guard to install-deps.sh** - `9b50f7d` (fix)
2. **Task 2: Validate hooks.json structure** - verification-only, no commit needed

## Files Created/Modified

- `scripts/install-deps.sh` - Added env-var fallbacks for PLUGIN_ROOT and DATA_DIR, added requirements.txt existence guard

## Decisions Made

- Fall back to `$(cd "$(dirname "$0")/.." && pwd)` for PLUGIN_ROOT — reliable, POSIX-portable, works whether the script is invoked by absolute or relative path
- Fall back to `${PLUGIN_ROOT}/.plugin-data` for DATA_DIR — keeps dev installs self-contained inside the project directory without polluting system paths
- Use `exit 0` (not `exit 1`) when requirements.txt is missing — a missing file in dev contexts is not an error, just a skip

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

- `scripts/install-deps.sh` exists and contains fallback logic
- Commit `9b50f7d` verified in git log
- Dev mode test: exits 0
- Plugin mode test: exits 0
- hooks.json: valid JSON

---
*Quick task: 260322-14r*
*Completed: 2026-03-22*
