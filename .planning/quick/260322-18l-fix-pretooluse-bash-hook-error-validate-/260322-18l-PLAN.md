---
phase: quick
plan: 260322-18l
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/validate-order.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "validate-order.sh runs without error when CLAUDE_PLUGIN_ROOT is unset (dev mode)"
    - "validate-order.sh still works identically when CLAUDE_PLUGIN_ROOT is set (plugin mode)"
    - "hooks.json remains valid JSON and references validate-order.sh correctly"
  artifacts:
    - path: "hooks/validate-order.sh"
      provides: "PreToolUse hook with dev-mode path fallback"
      contains: "PLUGIN_ROOT"
  key_links:
    - from: "hooks/hooks.json"
      to: "hooks/validate-order.sh"
      via: "PreToolUse command invocation"
      pattern: "validate-order.sh"
---

<objective>
Fix the PreToolUse Bash hook so validate-order.sh resolves correctly when CLAUDE_PLUGIN_ROOT is unset in dev context.

Purpose: Same bug as the SessionStart hook (quick task 260322-14r) -- when developing/testing the plugin locally, CLAUDE_PLUGIN_ROOT is not set by Claude Code, causing the hook command path to fail. Apply the identical env-var fallback pattern.
Output: A validate-order.sh that works in both plugin mode and dev mode.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/hooks.json
@hooks/validate-order.sh
@scripts/install-deps.sh (reference -- already has the fallback pattern to replicate)
@.planning/quick/260322-14r-fix-the-session-startup-hook-error/260322-14r-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-mode path fallback to validate-order.sh</name>
  <files>hooks/validate-order.sh</files>
  <action>
Add the same dual-context header pattern used in scripts/install-deps.sh. Insert after the `set -euo pipefail` line and before `INPUT=$(cat /dev/stdin)`:

1. Add a comment block explaining the two execution contexts (plugin mode vs dev mode), matching the style in install-deps.sh.

2. Add PLUGIN_ROOT resolution:
   ```
   PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
   ```
   validate-order.sh lives in hooks/, so dirname/$0/.. resolves to the repo root -- same relative logic as install-deps.sh (which lives in scripts/).

3. Update DATA_DIR to use the same fallback pattern as install-deps.sh instead of /tmp:
   Change `DATA_DIR="${CLAUDE_PLUGIN_DATA:-/tmp}"` to:
   ```
   DATA_DIR="${CLAUDE_PLUGIN_DATA:-${PLUGIN_ROOT}/.plugin-data}"
   ```
   This keeps dev-mode data in the project's .plugin-data/ directory (consistent with install-deps.sh) rather than scattering into /tmp.

4. Also update the CB_FLAG line (line 19) which currently has its own inline fallback `${CLAUDE_PLUGIN_DATA:-/tmp}` -- change it to use the DATA_DIR variable instead:
   ```
   CB_FLAG="${DATA_DIR}/circuit_breaker.flag"
   ```
   This eliminates the duplicated fallback and ensures all paths use the same resolved DATA_DIR.

5. Move the DATA_DIR and CB_FLAG declarations to be right after PLUGIN_ROOT (before INPUT=cat), so path resolution happens first, then stdin reading, then logic.

Do NOT change any of the grep pattern matching, jq output, sqlite3 queries, or exit codes. Only the path resolution at the top of the script changes.
  </action>
  <verify>
    <automated>bash -c 'cd /home/parz/projects/trading-bot && unset CLAUDE_PLUGIN_ROOT && unset CLAUDE_PLUGIN_DATA && echo "{\"tool_input\":{\"command\":\"echo hello\"}}" | bash hooks/validate-order.sh; echo "exit: $?"'</automated>
  </verify>
  <done>validate-order.sh exits 0 in dev mode (CLAUDE_PLUGIN_ROOT unset) for non-order commands. Script contains PLUGIN_ROOT fallback using dirname pattern. DATA_DIR falls back to .plugin-data/ not /tmp.</done>
</task>

<task type="auto">
  <name>Task 2: Verify hook integration and plugin-mode compatibility</name>
  <files>hooks/hooks.json</files>
  <action>
Verify (read-only, no changes expected):

1. Confirm hooks.json is valid JSON: `jq . hooks/hooks.json`
2. Confirm the PreToolUse entry still references validate-order.sh with the CLAUDE_PLUGIN_ROOT template variable (this is correct for plugin mode -- Claude Code expands it when the plugin is installed).
3. Test plugin-mode simulation: run validate-order.sh with CLAUDE_PLUGIN_ROOT explicitly set to the repo root and a non-order command -- should exit 0.
4. Test dev-mode with an order-like command pattern (no circuit breaker flag should exist, so it should also exit 0):
   ```
   unset CLAUDE_PLUGIN_ROOT
   echo '{"tool_input":{"command":"python submit_order.py"}}' | bash hooks/validate-order.sh
   ```

No file modifications expected for this task. If hooks.json needs changes, make them, but per the previous fix (260322-14r) it should not.
  </action>
  <verify>
    <automated>bash -c 'cd /home/parz/projects/trading-bot && jq . hooks/hooks.json > /dev/null && echo "hooks.json: valid" && CLAUDE_PLUGIN_ROOT=/home/parz/projects/trading-bot CLAUDE_PLUGIN_DATA=/tmp echo "{\"tool_input\":{\"command\":\"echo hi\"}}" | bash hooks/validate-order.sh && echo "plugin-mode: ok"'</automated>
  </verify>
  <done>hooks.json validates as JSON. validate-order.sh exits 0 in both dev mode and simulated plugin mode. No regressions in hook behavior.</done>
</task>

</tasks>

<verification>
- `unset CLAUDE_PLUGIN_ROOT && echo '{"tool_input":{"command":"echo hello"}}' | bash hooks/validate-order.sh` exits 0
- `unset CLAUDE_PLUGIN_ROOT && echo '{"tool_input":{"command":"python submit_order.py"}}' | bash hooks/validate-order.sh` exits 0 (no circuit breaker, no PDT trades)
- `CLAUDE_PLUGIN_ROOT=/home/parz/projects/trading-bot echo '{"tool_input":{"command":"echo hello"}}' | bash hooks/validate-order.sh` exits 0
- `jq . hooks/hooks.json` succeeds
- `grep 'PLUGIN_ROOT' hooks/validate-order.sh` shows the fallback pattern
</verification>

<success_criteria>
validate-order.sh runs without path errors in dev context (CLAUDE_PLUGIN_ROOT unset). The script resolves its own location via dirname fallback. DATA_DIR consistently uses .plugin-data/ in dev mode. No changes to hook logic, patterns, or exit behavior.
</success_criteria>

<output>
After completion, create `.planning/quick/260322-18l-fix-pretooluse-bash-hook-error-validate-/260322-18l-SUMMARY.md`
</output>
