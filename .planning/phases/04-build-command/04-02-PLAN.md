---
phase: 04-build-command
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/build_generator.py
  - tests/test_build_generator.py
autonomous: true
requirements:
  - CMD-08
  - CMD-09
  - DIST-04
  - DIST-05
must_haves:
  truths:
    - "Generated .env.template contains placeholder keys (ALPACA_API_KEY=, ALPACA_SECRET_KEY=) with no actual values"
    - "Generated .gitignore excludes .env, trading.db, __pycache__, *.pyc, and log files"
    - "Generated scripts can run standalone on a VPS by running python bot.py after filling in .env"
    - "Generated output includes a DEPLOY.md with cron entry and systemd unit file examples"
    - "No generated file contains a hardcoded API key value"
  artifacts:
    - path: "scripts/build_generator.py"
      provides: "Extended generator with .env.template, .gitignore, DEPLOY.md, run.sh"
      exports: ["generate_build"]
    - path: "tests/test_build_generator.py"
      provides: "Extended tests for secret management and deployment artifacts"
      contains: "test_env_template"
  key_links:
    - from: "scripts/build_generator.py"
      to: ".env.template"
      via: "writes template with placeholder keys"
      pattern: "ALPACA_API_KEY="
    - from: "scripts/build_generator.py"
      to: ".gitignore"
      via: "writes gitignore excluding secrets"
      pattern: "\\.env"
    - from: "scripts/build_generator.py"
      to: "DEPLOY.md"
      via: "writes deployment instructions"
      pattern: "systemd\\|cron"
---

<objective>
Extend the build generator to produce secret management files (.env.template, .gitignore), a standalone runner script, and deployment instructions (cron/systemd) so generated scripts are ready to deploy on any server.

Purpose: CMD-08 (env pattern for keys), CMD-09 (gitignore), DIST-04 (standalone execution), DIST-05 (cron/systemd instructions).
Output: Extended build_generator.py, .env.template/.gitignore generation, DEPLOY.md with cron+systemd examples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-command/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the generate_build function we are extending -->

From scripts/build_generator.py (created in Plan 01):
```python
def generate_build(config: dict, output_dir: Path) -> dict:
    """Generate standalone trading bot scripts from config.
    Returns {"output_dir": str, "files_generated": [str], "strategies_included": [str]}
    """
```

From requirements.txt:
```
alpaca-py==0.43.2
pandas-ta==0.4.71b0
pandas>=2.0
numpy>=1.26
APScheduler>=3.10,<4.0
pydantic-settings>=2.0
loguru>=0.7
python-dotenv>=1.0
rich>=13.0
```

Config keys relevant to deployment:
- config["paper_trading"] (bool) — determines ALPACA_PAPER value in .env.template
- config["scan_interval_seconds"] (int, default 60)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .env.template, .gitignore, requirements.txt, and DEPLOY.md generation</name>
  <files>scripts/build_generator.py, tests/test_build_generator.py</files>
  <read_first>
    - scripts/build_generator.py
    - tests/test_build_generator.py
    - requirements.txt
  </read_first>
  <behavior>
    - Test 1: generate_build() creates .env.template in output_dir with lines ALPACA_API_KEY=, ALPACA_SECRET_KEY=, ALPACA_PAPER=true (no actual key values)
    - Test 2: .env.template contains comment header explaining what each variable is
    - Test 3: generate_build() creates .gitignore in output_dir containing .env, trading.db, __pycache__, *.pyc, logs/
    - Test 4: generate_build() creates requirements.txt in output_dir with alpaca-py, pandas-ta, pandas, numpy, APScheduler, pydantic-settings, loguru, python-dotenv (no rich — not needed standalone)
    - Test 5: generate_build() creates DEPLOY.md in output_dir with both cron and systemd examples
    - Test 6: DEPLOY.md cron example contains "* * * * 1-5" (weekdays only) pattern reference
    - Test 7: DEPLOY.md systemd example contains [Unit], [Service], [Install] sections
    - Test 8: generate_build() creates run.sh in output_dir that loads .env and runs python bot.py
    - Test 9: run.sh has executable content (starts with #!/bin/bash, contains "python bot.py")
    - Test 10: No generated file in output_dir contains a string matching a real API key pattern (no ALPACA_API_KEY=PK... or ALPACA_SECRET_KEY=... with values longer than 0 chars after =)
    - Test 11: files_generated in return dict includes .env.template, .gitignore, requirements.txt, DEPLOY.md, run.sh
  </behavior>
  <action>
    Extend generate_build() in scripts/build_generator.py to also generate these files in output_dir:

    1. **.env.template** — Plaintext file with:
       ```
       # Alpaca API Credentials
       # Get your keys at: https://app.alpaca.markets/
       # Paper trading keys: https://app.alpaca.markets/paper/dashboard/overview
       ALPACA_API_KEY=
       ALPACA_SECRET_KEY=

       # Set to "true" for paper trading (simulated), "false" for live trading
       ALPACA_PAPER=true
       ```
       The ALPACA_PAPER value defaults to "true" always in the template (user changes to "false" manually for live). Never write actual key values.

    2. **.gitignore** — Contains:
       ```
       # Secrets — NEVER commit
       .env

       # Database
       trading.db
       trading.db-wal
       trading.db-shm

       # Python
       __pycache__/
       *.pyc
       *.pyo
       .venv/
       venv/

       # Logs
       logs/
       *.log

       # IDE
       .vscode/
       .idea/
       ```

    3. **requirements.txt** — Contains only the runtime deps needed for standalone execution (not rich, not dev tools):
       ```
       alpaca-py==0.43.2
       pandas-ta==0.4.71b0
       pandas>=2.0
       numpy>=1.26
       APScheduler>=3.10,<4.0
       pydantic-settings>=2.0
       loguru>=0.7
       python-dotenv>=1.0
       ```

    4. **run.sh** — Bash launcher script:
       ```bash
       #!/bin/bash
       # Trading Bot Launcher
       # Loads .env and starts the bot

       set -euo pipefail
       cd "$(dirname "$0")"

       if [ ! -f .env ]; then
           echo "ERROR: .env file not found. Copy .env.template to .env and fill in your API keys."
           exit 1
       fi

       # Export all variables from .env
       set -a
       source .env
       set +a

       echo "Starting trading bot (paper=$ALPACA_PAPER)..."
       python bot.py
       ```

    5. **DEPLOY.md** — Deployment instructions with three sections:

       **Quick Start:**
       - Copy directory to server
       - cp .env.template .env and fill in keys
       - pip install -r requirements.txt (or uv pip install -r requirements.txt)
       - python bot.py

       **Cron Setup (simple):**
       - The bot runs its own internal scheduler (APScheduler, 60-second loop)
       - Cron is only needed for auto-restart on reboot
       - Example: @reboot cd /path/to/trading-bot-standalone && ./run.sh >> logs/bot.log 2>&1
       - Note: The bot checks market hours internally — safe to run 24/7, it skips closed hours

       **Systemd Setup (recommended for VPS):**
       ```ini
       [Unit]
       Description=Autonomous Trading Bot
       After=network-online.target
       Wants=network-online.target

       [Service]
       Type=simple
       WorkingDirectory=/path/to/trading-bot-standalone
       ExecStart=/usr/bin/python3 bot.py
       EnvironmentFile=/path/to/trading-bot-standalone/.env
       Restart=on-failure
       RestartSec=30

       [Install]
       WantedBy=multi-user.target
       ```
       Include instructions: sudo cp trading-bot.service /etc/systemd/system/, sudo systemctl enable trading-bot, sudo systemctl start trading-bot

    Update the files_generated list in the return dict to include all new files.

    Also add tests for all the above to tests/test_build_generator.py. Tests should use tmp_path fixture and a minimal valid config dict.
  </action>
  <verify>
    <automated>cd /home/parz/projects/trading-bot && python -m pytest tests/test_build_generator.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "env.template" scripts/build_generator.py
    - grep -q "gitignore" scripts/build_generator.py
    - grep -q "DEPLOY" scripts/build_generator.py
    - grep -q "run.sh" scripts/build_generator.py
    - grep -q "requirements.txt" scripts/build_generator.py
    - grep -q "systemd" scripts/build_generator.py
    - grep -q "cron" scripts/build_generator.py
    - grep -q "ALPACA_API_KEY=" scripts/build_generator.py
    - grep -q "test_env_template" tests/test_build_generator.py
    - grep -q "test_gitignore" tests/test_build_generator.py
    - grep -q "test_deploy" tests/test_build_generator.py
    - All tests pass
  </acceptance_criteria>
  <done>
    generate_build() produces a fully self-contained standalone directory. .env.template has placeholder keys with no values. .gitignore excludes secrets and build artifacts. requirements.txt has runtime deps. DEPLOY.md has quick-start, cron, and systemd instructions. run.sh is a ready-to-use launcher. No generated file contains hardcoded API key values. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /build command to reference all generated artifacts</name>
  <files>commands/build.md</files>
  <read_first>
    - commands/build.md
    - scripts/build_generator.py
  </read_first>
  <action>
    Update commands/build.md to include complete next-steps guidance that references all artifacts generated by the extended build_generator:

    In the "Next steps guidance" section (step 5 of the prompt), update to:

    After reporting the generated files, tell the user:

    1. "Your standalone trading bot has been generated at: {output_dir}"
    2. "Files generated:" — list all files from result["files_generated"]
    3. "Strategies included:" — list from result["strategies_included"]
    4. "To deploy on a server:"
       - "1. Copy the entire directory to your server"
       - "2. Run: cp .env.template .env"
       - "3. Edit .env and add your Alpaca API keys"
       - "4. Run: pip install -r requirements.txt"
       - "5. Run: python bot.py (or use ./run.sh)"
       - "6. See DEPLOY.md for cron/systemd setup for unattended operation"
    5. Security reminder: "Your .env file contains secrets. The included .gitignore will prevent accidentally committing it to git."
    6. Paper trading note: "The bot defaults to paper trading mode. To switch to live trading, set ALPACA_PAPER=false in your .env file."
  </action>
  <verify>
    <automated>cd /home/parz/projects/trading-bot && grep -q "DEPLOY.md" commands/build.md && grep -q "env.template" commands/build.md && grep -q "run.sh" commands/build.md && grep -q "gitignore" commands/build.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DEPLOY.md" commands/build.md
    - grep -q "env.template" commands/build.md
    - grep -q "run.sh" commands/build.md
    - grep -q "gitignore" commands/build.md
    - grep -q "paper" commands/build.md (paper trading note)
    - grep -q "pip install" commands/build.md (install instructions)
  </acceptance_criteria>
  <done>
    /build command's output guidance references all generated artifacts: .env.template, .gitignore, requirements.txt, run.sh, DEPLOY.md. Includes security reminder about .env and paper trading default note.
  </done>
</task>

</tasks>

<verification>
- Full test suite passes: python -m pytest tests/test_build_generator.py -x -v
- generate_build() output directory contains: bot.py, types.py, market_scanner.py, order_executor.py, risk_manager.py, state_store.py, portfolio_tracker.py, strategies/, config.json, .env.template, .gitignore, requirements.txt, DEPLOY.md, run.sh
- .env.template has ALPACA_API_KEY= with NO value after the equals sign
- .gitignore includes .env
- DEPLOY.md includes both cron and systemd sections
- commands/build.md references all deployment artifacts
- No generated file contains hardcoded secrets
</verification>

<success_criteria>
- Generated standalone directory is fully self-contained — can be copied to any Linux server
- .env pattern enforced: API keys loaded from environment only, template provided for user
- .gitignore prevents accidental secret commits
- DEPLOY.md gives both cron and systemd deployment paths
- run.sh provides one-command launch with .env loading
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-command/04-02-SUMMARY.md`
</output>
